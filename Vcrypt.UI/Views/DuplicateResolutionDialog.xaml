<Window x:Class="Vcrypt.UI.Views.DuplicateResolutionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Conflict" Height="250" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="#1E1E1E" CornerRadius="12" BorderBrush="#333333" BorderThickness="1" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="File Already Exists" Foreground="#FFFFFF" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

            <StackPanel Grid.Row="1">
                <TextBlock Foreground="#CCCCCC" TextWrapping="Wrap" FontSize="14">
                    <Run Text="The destination already contains a file named "/>
                    <Run x:Name="FileNameRun" FontWeight="Bold" Foreground="#4A90E2" Text="filename.txt"/>
                    <Run Text="."/>
                </TextBlock>

                <Border x:Name="IdenticalWarningBorder" Background="#2A3026" BorderBrush="#4C8C38" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,15,0,0" Visibility="Collapsed">
                    <TextBlock Foreground="#7ED321" TextWrapping="Wrap">
                        🤖 AI Hash Match: The contents of these two files are perfectly identical. It is safe to skip.
                    </TextBlock>
                </Border>
            </StackPanel>

            <CheckBox x:Name="ApplyToAllCheckBox" Grid.Row="2" Content="Do this for all remaining conflicts" Foreground="#AAAAAA" Margin="0,15,0,15"/>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Replace" Background="#4A90E2" Foreground="White" BorderThickness="0" Padding="10" Click="Replace_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Grid.Column="2" Content="Skip" Background="#333333" Foreground="White" BorderThickness="0" Padding="10" Click="Skip_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
