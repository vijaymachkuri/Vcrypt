<UserControl x:Class="Vcrypt.UI.AiChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:Vcrypt.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="350"
             Loaded="UserControl_Loaded"
             Unloaded="UserControl_Unloaded">
    <UserControl.Resources>
        <converters:StringToVisConverter x:Key="StringToVisConverter" />
        <converters:BoolToVisConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>

    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" Padding="15">
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="Bot24" FontSize="20" Margin="0,0,10,0"/>
                <TextBlock Text="Gem AI" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            
            <!-- LOADING STATE -->
            <Grid Visibility="{Binding CurrentView, Converter={StaticResource StringToVisConverter}, ConverterParameter='Loading'}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ui:ProgressRing IsIndeterminate="True" Width="40" Height="40" Margin="0,0,0,15"/>
                    <TextBlock Text="Initializing AI Engine..." FontSize="14" FontWeight="SemiBold" TextAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- DOWNLOAD STATE -->
            <Grid Visibility="{Binding CurrentView, Converter={StaticResource StringToVisConverter}, ConverterParameter='Download'}" Margin="20">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="🤖" FontSize="48" TextAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="Gem" FontSize="24" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="To ensure 100% privacy, Gem runs entirely on your PC. No data is ever sent to the cloud." TextWrapping="Wrap" TextAlignment="Center" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,20"/>
                    
                    <TextBlock Text="{Binding DownloadStatus}" TextAlignment="Center" Margin="0,0,0,10" FontWeight="SemiBold" FontSize="12"/>
                    
                    <Grid Margin="0,0,0,20">
                        <ProgressBar Value="{Binding DownloadProgress}" Minimum="0" Maximum="100" Height="6" Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisConverter}}" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}"/>
                    </Grid>
                    
                    <ui:Button Content="Download Model (2.4 GB)" Appearance="Primary" Command="{Binding DownloadModelCommand}" HorizontalAlignment="Stretch" Padding="0,8" FontSize="14" Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisConverter}, ConverterParameter=Inverse}" />
                    <ui:Button Content="Cancel Download" Appearance="Danger" Command="{Binding CancelDownloadCommand}" HorizontalAlignment="Stretch" Padding="0,8" FontSize="14" Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisConverter}}" />
                </StackPanel>
            </Grid>

            <!-- CHAT STATE -->
            <Grid Visibility="{Binding CurrentView, Converter={StaticResource StringToVisConverter}, ConverterParameter='Chat'}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Chat History -->
                <ListView x:Name="ChatListView" Grid.Row="0" ItemsSource="{Binding Messages}" Background="Transparent" BorderThickness="0" Margin="10,0,10,10" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,5,0,5">
                                <!-- AI Bubble -->
                                <Border Background="{DynamicResource ControlFillColorSecondaryBrush}" CornerRadius="12,12,12,0" Padding="12" MaxWidth="280" HorizontalAlignment="Left" Visibility="{Binding IsAi, Converter={StaticResource BoolToVisConverter}}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Sender}" FontWeight="SemiBold" FontSize="10" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding Text}" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap" FontSize="13" LineHeight="18"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- User Bubble -->
                                <Border Background="{DynamicResource SystemAccentColorPrimaryBrush}" CornerRadius="12,12,0,12" Padding="12" MaxWidth="280" HorizontalAlignment="Right" Visibility="{Binding IsAi, Converter={StaticResource BoolToVisConverter}, ConverterParameter=Inverse}">
                                    <TextBlock Text="{Binding Text}" Foreground="White" TextWrapping="Wrap" FontSize="13" LineHeight="18"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                <!-- Chat Input Area -->
                <Border Grid.Row="1" Background="{DynamicResource ControlFillColorSecondaryBrush}" Padding="10" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <ui:TextBox Grid.Column="0" Text="{Binding InputText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Ask Gem anything..." Margin="0,0,8,0" VerticalAlignment="Center" AcceptsReturn="False" KeyDown="TextBox_KeyDown" />
                        
                        <ui:Button Grid.Column="1" Command="{Binding SendMessageCommand}" IsEnabled="{Binding IsChatting, Converter={StaticResource BoolToVisConverter}, ConverterParameter=Inverse}" Appearance="Primary" Padding="8">
                            <ui:SymbolIcon Symbol="Send24" FontSize="16" />
                        </ui:Button>
                    </Grid>
                </Border>
            </Grid>

        </Grid>
    </Grid>
</UserControl>
